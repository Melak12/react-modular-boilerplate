import { useCurrentUser } from "../auth";
// Test Authentication Module
describe("Authentcation Tests", () => {
    it('should return the current user', () => {
        expect(true).toBeTruthy();
    });
});

export { }